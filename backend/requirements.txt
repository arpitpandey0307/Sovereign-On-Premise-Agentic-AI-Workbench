fastapi>=0.115
starlette>=0.40         # imported directly for the shared error envelope
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy>=2.0.36
alembic>=1.14
psycopg[binary]>=3.2
PyJWT>=2.9
bcrypt>=4.2
python-multipart>=0.0.12
httpx>=0.27

# Part 03 - document intelligence
PyMuPDF>=1.24            # native-text PDF extraction
pytesseract>=0.3.13      # wrapper; needs the Tesseract binary installed separately
Pillow>=10.4
neo4j>=5.25              # graph + vector index

# Part 04 - orchestration, sandbox and deterministic artifact generation
langgraph>=0.2            # explicit state machine, checkpoints, human-in-loop
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0
docker>=7.1               # sandbox runner; talks to the local daemon only
minio>=7.2                # object storage; the endpoint is validated as local
